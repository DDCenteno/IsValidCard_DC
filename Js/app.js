numberInside= [prompt("Ingrese su numero de tarjeta de credito:")];
isValidCard(numberInside);
function isValidCard (number){
    numberParse = number.parseInt;// se vuelve entero los digitos que ingresaron del prompt.
    
}